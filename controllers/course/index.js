const getAll = () => {
  res.send("Work in progress ...");
};

const getCourse = () => {
  res.send("Work in progress ...");
};

const updateCourse = () => {
  res.send("Work in progress ...");
};

const deleteCourse = () => {
  res.send("Work in progress ...");
};

module.exports = {
  getAll,
  getCourse,
  updateCourse,
  deleteCourse,
};
