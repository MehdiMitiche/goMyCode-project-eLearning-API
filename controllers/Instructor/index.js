const getAll = () => {
  res.send("Work in progress ...");
};

const getInstructor = () => {
  res.send("Work in progress ...");
};

const updateInstructor = () => {
  res.send("Work in progress ...");
};

const deleteInstructor = () => {
  res.send("Work in progress ...");
};

module.exports = {
  getAll,
  getInstructor,
  updateInstructor,
  deleteInstructor,
};
