const getProgression = () => {
  res.send("Work in progress ...");
};

const progress = () => {
  res.send("Work in progress ...");
};

const start = () => {
  res.send("Work in progress ...");
};

module.exports = {
  getProgression,
  progress,
  start,
};
