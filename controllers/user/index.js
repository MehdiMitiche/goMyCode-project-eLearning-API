const getAll = () => {
  res.send("Work in progress ...");
};

const getUser = () => {
  res.send("Work in progress ...");
};

const updateUser = () => {
  res.send("Work in progress ...");
};

const deleteUser = () => {
  res.send("Work in progress ...");
};

module.exports = {
  getAll,
  getUser,
  updateUser,
  deleteUser,
};
